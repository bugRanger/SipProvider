using SipProvider.SIP;

namespace SipProvider
{
    class Program
    {
        static void Main(string[] args)
        {
            //var msg = new SipMessage();
            //msg.Append(new Expires(12));


            //ISipMessage imsg = msg;

            //if (imsg.TryGet(out Expires expires))
            //{
            //    int t = expires.Value;
            //}
        }
    }
}
